package com.example.cross_currency_risk.repository;


import com.example.cross_currency_risk.domain.RiskLimit;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.Optional;

public interface RiskLimitRepository
        extends JpaRepository<RiskLimit, Long> {

    Optional<RiskLimit> findByCurrency(String currency);
}
